import {useEffect,useState} from "react";
type Finding={finding_id:string;source:string;finding_type:string;title:string;summary:string;user_name:string;department:string;hostname:string;created_at:string;reasons:string[];baseline:Record<string,unknown>;related_event_ids:string[];observed:Record<string,unknown>};
const sources=[['','전체'],['detections','Detection'],['xdr','Email XDR'],['inbound','Inbound'],['outbound','Outbound'],['dlp','DLP'],['firewall','Firewall']];const types=[['','전체 유형'],['NEW_BEHAVIOR','새로운 행동'],['FREQUENCY_SPIKE','활동 증가'],['SIMILAR_GROUP','비슷한 이벤트']];
export function MachineLearningPage(){const [source,setSource]=useState(''),[type,setType]=useState(''),[start,setStart]=useState(''),[end,setEnd]=useState(''),[items,setItems]=useState<Finding[]>([]),[status,setStatus]=useState('확인 중'),[selected,setSelected]=useState<Finding|null>(null),[message,setMessage]=useState('');const load=()=>{const q=new URLSearchParams({source,findingType:type,start,end});fetch(`/api/learner/findings?${q}`).then(r=>r.json()).then(p=>setItems(p.data||[]));fetch('/api/system-info/process-status').then(r=>r.json()).then(p=>setStatus(p.data?.learner?.status||'unavailable'))};useEffect(load,[source,type,start,end]);const run=async(mode:string)=>{const r=await fetch('/api/learner/jobs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mode,sources:source?[source]:undefined,start,end})}),p=await r.json();setMessage(r.ok?`분석 작업 요청됨 · ${p.data.id}`:(p.error?.message||'요청 실패'))};return <><header className="topbar"><div><p className="breadcrumb">Lab / Machine Learning</p><h1>Machine Learning</h1></div><span>Learner 상태 · {status}</span></header><section className="panel learner-toolbar"><div className="learner-source-tabs">{sources.map(([key,label])=><button className={source===key?'active':''} onClick={()=>setSource(key)} key={key}>{label}</button>)}</div><select value={type} onChange={e=>setType(e.target.value)}>{types.map(([key,label])=><option value={key} key={key}>{label}</option>)}</select><input type="date" value={start} onChange={e=>setStart(e.target.value)}/><b>~</b><input type="date" value={end} onChange={e=>setEnd(e.target.value)}/><button onClick={()=>run('incremental')}>새 데이터 분석</button><button onClick={()=>run('full')}>전체 다시 분석</button><small>{message}</small></section><section className="learner-findings">{items.length===0&&<div className="panel learner-empty">저장된 분석 결과가 없습니다. Learner 분석을 실행하세요.</div>}{items.map(f=><article className="config-card learner-finding" key={f.finding_id}><header><span>{types.find(x=>x[0]===f.finding_type)?.[1]||f.finding_type}</span><time>{f.created_at}</time></header><h2>{f.title}</h2><b>{[f.user_name,f.department,f.hostname].filter(Boolean).join(' · ')||'매핑되지 않은 이벤트'}</b><p>{f.summary}</p><h3>왜 표시됐나요?</h3><ul>{f.reasons.map((x,i)=><li key={i}>{x}</li>)}</ul><footer><span>관련 이벤트 {f.related_event_ids.length.toLocaleString()}건</span><button onClick={()=>setSelected(f)}>상세 / Raw / 과거 기준</button></footer></article>)}</section>{selected&&<div className="modal-backdrop" onClick={()=>setSelected(null)}><section className="detail-modal" onClick={e=>e.stopPropagation()}><header><h2>{selected.title}</h2><button onClick={()=>setSelected(null)}>×</button></header><pre>{JSON.stringify({observed:selected.observed,baseline:selected.baseline,reasons:selected.reasons,relatedEvents:selected.related_event_ids},null,2)}</pre></section></div>}</>}
